import {Container, Title} from "@/ui";

export const IndexPage = (): JSX.Element => {
    return (
        <section>
            <Container>
                <Title>Home page</Title>
            </Container>
        </section>
    )
}